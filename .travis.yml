language: cpp

compiler:  
    - gcc
    - clang

install:
    - sudo apt-get -qq update
    - sudo apt-get install -y libeigen3-dev libopencv-dev

before_script:  
    - mkdir build
    - cd build
    - cmake .. -DBUILD_STANDALONE=1

script: make